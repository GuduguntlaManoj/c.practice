#include <stdio.h>    
    void main()
    {
      int num;
     
      printf("Enter a number\n");
      scanf("%d", &num); // %d is used for an integer
     
      printf("The number is: %d\n", num);
     
    }
